# Financial Analyzer - Kiro Power

中国A股财务分析工具，支持自动获取财务数据、生成分析报告和投资建议。

## 功能

- 📊 自动获取A股财务数据 (支持 AKShare/Tushare)
- 📈 生成 Excel + TXT 双格式报告
- 💰 DCF 估值和唐朝估值法
- 🔬 敏感性分析（6个可调参数）
- ✅ 数据验证和可靠性评分

## 安装

### 前置条件

1. **安装 Node.js** (v18+)
2. **安装 Rust** (用于编译分析器)
3. **安装 Python 数据源** (推荐):
   ```bash
   pip3 install akshare
   ```

### 安装步骤

```bash
# 1. 克隆或下载项目
cd /path/to/financial-analyzer-mcp
npm install
npm run build

# 2. 编译 Rust 分析器
cd ../financial-analyzer
cargo build --release

# 3. 配置到 Kiro
# 编辑 ~/.kiro/mcp.json
{
  "mcpServers": {
    "financial-analyzer": {
      "command": "node",
      "args": ["/path/to/financial-analyzer-mcp/build/index.js"]
    }
  }
}
```

## 使用

在 Kiro CLI 中直接对话:

```
分析茅台 600519.SH 2021-2019年的财务数据
```

```
用 akshare 分析五粮液 000858.SZ 最近3年的数据并启用验证
```

```
分析 600519.SH 2019-2017年，设置折现率0.10，永续增长率0.05
```

## 工具参数

### analyze_stock

**必需参数:**
- `stock_code`: 股票代码，如 600519.SH
- `years`: 年份，如 "2021,2020,2019"

**可选参数:**
- `source`: mock/akshare/tushare，默认 akshare
- `output`: 输出路径
- `enable_validation`: 启用数据验证
- `discount_rate`: 敏感性分析 - 折现率
- `perpetual_growth_rate`: 敏感性分析 - 永续增长率
- `fcf_growth_rate`: 敏感性分析 - FCF增长率
- `net_profit_growth_rate`: 敏感性分析 - 净利润增长率
- `low_risk_free_rate`: 敏感性分析 - 无风险收益率(低估)
- `high_risk_free_rate`: 敏感性分析 - 无风险收益率(高估)

## 输出

- `{股票代码}_财务分析.xlsx` - Excel 详细报告（含敏感性分析工作表）
- `{股票代码}_财务分析.txt` - 文本摘要（含敏感性分析结果）

## 许可证

MIT
